Next.js sample using Firebase auth
===

Reference : https://zenn.dev/k_logic24/articles/react-auth-with-firebase

**Run the app**

Configure settings
```bash
$ cp dot.env.local .env.local
$ vim .env.local
```
Run server
```bash
$ yarn install
$ yarn dev
```

Access to `http://localhost:3000`
